**Cumulative performance review — 5 September 2026**

The five findings in the goal are addressed and the cumulative review is complete. Review covered all production changes from `5245ab7` through `89db703`, their callers and tests, and the two subsequent fixes through `d090b9e`. No remaining blocking finding was identified within this scope. The original audit's nine prioritized findings are now closed; the queue-policy recommendation is resolved by measurement and a documented deferral.

| Goal finding | Checkpoints | Outcome |
| --- | --- | --- |
| Comparison preparation | `b5bc3b0` | Reuse completed projections, sort compact offsets, remove temporary collections, and cancel traversals. |
| Main-actor navigation/publication | `53d68aa`, `e87bebe` | Prepare large directories asynchronously and transfer retired large buffers to background cleanup. |
| Metadata reads | `3e568ae` | Reuse status fields within a load; preserve independent fresh identity validation and explicit overrides. |
| Scanner finalization | `dd912a6` | Check cancellation during sibling comparisons and ID-index conversion; preserve single-pass aggregation. |
| Scanner allocations/frontier | `89db703` | Omit unused ASCII leaf-name buffers and measure 10,000-directory / two-million-file frontiers. |

**Review findings fixed**

`2d26451` fixes a remaining shared-sort cancellation gap: small inputs bypassed checks entirely, and each 16,384-element chunk still used an unchecked standard sort. A regression test failed on both paths before the fix. Both now reuse the checked in-place sorter; stable order across chunk boundaries is tested. This also consolidates the comparison-check invariant instead of maintaining a second mechanism.

`d090b9e` fixes signed device-ID conversion. Expanding status reuse made the old checked `UInt64(st_dev)` conversion apply to ordinary metadata loads too. Status conversion now preserves signed device bit patterns consistently with native enumeration instead of trapping when the sign bit is set. The stat-to-status initializer is tested with a negative device ID, zero link count, and extreme block counts; allocation clamping and file flags remain correct.

The review also checked completed-only projection reuse, dataset/filter invalidation, stale-result suppression, selection reconciliation, small-directory behavior, explicit deferred loading, background release ownership/locking, shared backing-tree accounting, resource-value precedence, no-follow symlink identity, native-name lifetimes, Unicode collision handling, directory replacement validation, aggregation order, and Package.swift source registration. The release worker destroys values outside its lock, and existing cache admission remains separate from the UI producer gate. There are no new dependencies or user-facing strings.

**Final validation**

The complete core suite executed **922 tests: 33 skipped, zero failures**. Skips include opt-in benchmarks and filesystem-dependent Unicode cases. The focused Release suite executed **136 tests: two skipped, zero failures**. The full Debug app build passed at the required deterministic DerivedData path. Whitespace and result-file checks passed. Documentation-only updates after these checks do not change the tested source.

Ten additional fresh Release processes compared the final review fixes with `89db703`: three 100,000-row comparison pairs, one million-row comparison pair, and one million-row browser pair. Processes ran sequentially after builds and the UI test app had exited. All assertions and fingerprints matched.

| Sort check | Before | After |
| --- | ---: | ---: |
| Comparison service, 100k rows, median of three | 42.757 ms | 42.907 ms |
| Comparison service, 1m rows, one pair | 629.770 ms | 637.687 ms |
| Comparison model sort refresh, 1m rows, one pair | 709.661 ms | 670.030 ms |
| Browser sort, 1m rows, one pair | 1,698.497 ms | 1,731.910 ms |
| Browser end-to-end refresh, 1m rows, one pair | 2,008.397 ms | 2,055.145 ms |

The added checks have small measured overhead, approximately 1–2% in the million-row service/browser sorts; single pairs are not statistical guarantees. Earlier offset-sorting and projection-reuse gains remain intact. Both browser cancellation probes passed. [Final raw records](/Users/colin/Programming/Radix/docs/performance-audit/review-results-2026-09-05.json) preserve all phases and validation metadata.

**UI smoke test and limits**

Manual testing used only `/Users/colin/Programming/Radix/.build/xcode-derived-data/Build/Products/Debug/Radix.app`, with exactly one process verified before interaction. The app completed the two-million-file fixture with 10,003 directories and zero warnings. It opened the million-file directory, filtered its last file (`file-00999999.dat`), cleared the query and returned to the parent while results refreshed, navigated into the 10,000-directory branch and a 100-file child, and reported no changes on rescan. The test app was then quit and its exit verified.

Computer Use encountered file-picker and accessibility timeouts. Selecting from the fixture's parent avoided asking the system picker to list a million files. Screenshots verified the filter result when accessibility calls timed out. Two one-second process samples showed the main thread mostly waiting in its event loop rather than doing sustained scanner work: [first sample](/Users/colin/Programming/Radix/docs/performance-audit/review-radix-ui-sample.txt), [filter sample](/Users/colin/Programming/Radix/docs/performance-audit/review-radix-ui-filter-sample.txt). These samples do not establish frame-time or interaction-latency bounds.

The Debug process samples reported approximately 5.6–5.7 GB RSS after the two-million-file scan and million-row table interactions. This includes active models, SwiftUI/AppKit, picker/accessibility interactions, and allocator retention, with no paired baseline or isolated attribution. The completed-cache policy and retired-buffer gate do not cap the active working set. Live SwiftUI frame/body tracing and attribution of large-window memory remain useful follow-up profiling, alongside cold-cache/network/provider-backed storage, allocation-event totals, and actual lock wait time. These are the broader audit boundaries, not unfinished requirements of the five-finding goal.

Generated filesystem fixtures were removed after all measurements and manual checks. Their paths remain in records for provenance. Scanned user data was never modified or removed. No changes were pushed or published.
